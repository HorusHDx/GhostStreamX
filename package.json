{
  "name": "ghoststreamx",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "GhostStreamX - Web stream propio tipo Netflix consumiendo APIs (TMDB + agregadores).",
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"node api/index.js\" \"npm --prefix frontend run dev\"",
    "dev:api": "node api/index.js",
    "dev:web": "npm --prefix frontend run dev",
    "install:all": "npm install && npm --prefix frontend install"
  },
  "dependencies": {
    "concurrently": "^9.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=18"
  }
}
